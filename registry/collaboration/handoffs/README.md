# Collaboration Handoffs

Dated English handoff notes between the Claude/OMC lane and the Codex/OMX lane.

Naming: `YYYYMMDD-HHMM-<from>-to-<to>-<slug>.md` where `<from>`/`<to>` is `claude` or `codex`.

See `docs/codex-claude-collaboration-protocol.md` for the required sections in each handoff.
