# Brief: OMC<->OMX Interop Proof

Goal: prove the Codex-Claude collaboration layer with a REAL runtime, not just
installed capability. Run `omc interop` inside a real tmux session and drive a
real OMC->OMX task round-trip answered by the real Codex model.

Start: `./run-interop-proof.sh`

Scenario type: infrastructure proof (collaboration runtime).
